// uuid > private/widgets > data

type initialState_widget = {
    name: string,
    active: boolean,
    widge_type: string,
    p_key: string,
    data: []
}

type initialState_user = {
    uuid: string,
    loginType: string,
    email: string
}

export {

}